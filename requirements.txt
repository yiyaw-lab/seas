# Argo V2 observation generation (src/argo_observe.py)
python-dotenv   # load OPENAI_API_KEY / ANTHROPIC_API_KEY / ARGO_MODEL from .env
openai>=1.55     # gpt-* / o* models (>=1.55: fixes httpx>=0.28 'proxies' TypeError)
anthropic>=0.105  # claude-* models + beta.messages MCP connector (argo_observe.chat_with_mcp)

# Telegram delivery (src/send_telegram.py)
certifi         # CA bundle for TLS verification (macOS framework Python lacks roots)

# Fresh signal ingestion (src/fetch_signals.py)
feedparser      # robust RSS/Atom parsing (stdlib fallback exists if absent)

# Two-way Telegram chat webhook (src/argo_webhook.py)
flask           # WSGI app receiving Telegram webhook POSTs

# Agentic MCP server + ASGI hosting (src/argo_mcp_server.py, create_asgi_app)
mcp             # FastMCP streamable-HTTP server (Argo's tools)
uvicorn         # ASGI server running webhook + MCP in one process
asgiref         # WsgiToAsgi: serve the Flask webhook under ASGI
starlette       # ASGI routing/mounting + bearer-auth middleware
