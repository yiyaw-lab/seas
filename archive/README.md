# Archive

This directory will eventually hold superseded SEAS generations (e.g. the
capability engine, manual scoring, and the Gen-2 experiment flow) so their
history is preserved without cluttering the live system.

- **Nothing currently in `archive/` is part of the live runtime.**
- **No files have been moved here yet.** This directory only contains this note.

The live architecture is described in the top-level [README](../README.md):

```
Signal → Opportunity → Experiment → Finding → Theory
```
